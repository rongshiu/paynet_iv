# Generate a local key with:
#
#   printf 'PII_HMAC_KEY=%s\n' "$(openssl rand -hex 32)" > .env
#
# Used to tokenise card and transaction IDs. Minimum 32 characters; do not
# commit the value.
#
# A new key produces new tokens, so production keys need versioning.
PII_HMAC_KEY=
